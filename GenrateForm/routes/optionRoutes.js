const express = require("express");
const app = express();
const router = express.Router();

const { } = require("../controller/optionController");

// router.get('/records',studentData);
// router.get('/pagination',pagination);

module.exports= router;